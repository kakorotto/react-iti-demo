{
  "buildCommand": "npm ci && npm test -- --watchAll=false && npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "installCommand": "npm ci",
  "rewrites": [{ "source": "/((?!static|.*\\..*).*)", "destination": "/index.html" }]
}
